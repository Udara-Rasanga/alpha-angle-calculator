# alpha-angle-calculator
Simple Python program to calculate the angle alpha and the angle formed by force P using trigonometric formulas. It takes P, Q, and theta as inputs, converts degrees to radians, and computes accurate results using the math library with error handling for zero inputs.
